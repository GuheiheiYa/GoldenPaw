# GoldenPaw 未完成功能清单

整理时间：2026-05-28

---

## 一、缺失页面/功能

### 1. 首页 (index.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 🔍 搜索按钮 | 无功能 | 需要搜索页面或弹窗 |
| 🔔 通知按钮 | 无功能 | 需要通知列表页面 |
| "管理" (预算) | 跳转 `/pages/budget/budget` | **页面不存在**，需创建预算管理页 |
| "全部" (存钱目标) | 跳转 `/pages/goal/goal` | **页面不存在**，需创建存钱目标列表页 |
| "查看全部" (流水) | 跳转 `/pages/detail/detail` | ✅ 已实现 |
| 流水项点击 | 无功能 | 需要交易详情页 |

### 2. 明细页 (detail.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 🔍 搜索按钮 | 无功能 | 需要搜索功能 |
| ⚙ 筛选按钮 | 无功能 | 需要筛选弹窗（按金额/日期/账户筛选） |
| 交易项点击 | `console.log` | **需要交易详情/编辑页** |
| 左滑删除 | 未实现 | 需要 SwipeCell 删除功能 |

### 3. 资产页 (assets.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 📊 图表按钮 | 无功能 | 可跳转资产趋势详情 |
| ⚙ 设置按钮 | 无功能 | 需要资产设置页 |
| 账户卡片点击 | 无功能 | 需要**账户详情页** |
| "添加账户" | 无功能 | 需要**添加账户页面** |

### 4. AI记账页 (ai.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 语音记账卡片 | 无功能 | 需要录音权限和语音识别 |
| 聊天记账卡片 | 无功能 | 需要AI对话功能 |
| 票据识别卡片 | 无功能 | 需要相机权限和OCR |
| 智能分析卡片 | 无功能 | 需要数据分析页面 |
| 发送按钮 | 无功能 | 需要聊天消息发送 |
| 建议话术点击 | `console.log` | 需要填入输入框 |

### 5. 我的页面 (profile.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 导出数据 | `console.log` | 需要导出功能页面 |
| 导入数据 | `console.log` | 需要导入功能页面 |
| 云同步 | `console.log` | 需要同步状态页面 |
| 清空数据 | `console.log` | 需要确认弹窗 |
| 主题颜色 | `console.log` | 需要**主题设置页** |
| 提醒设置 | `console.log` | 需要**提醒设置页** |
| 密码/指纹锁 | `console.log` | 需要**安全设置页** |
| 分类管理 | `console.log` | 需要**分类管理页** |
| 账户管理 | `console.log` | 需要**账户管理页** |
| 币种设置 | `console.log` | 需要**币种设置页** |
| 记账周期 | `console.log` | 需要**周期设置页** |
| 帮助中心 | `console.log` | 需要**帮助页面** |
| 评分鼓励 | `console.log` | 需要跳转应用商店 |
| 用户协议 | `console.log` | 需要**协议页面** |

### 6. 报表页 (report.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 周期选择 | 只切换显示 | 需要重新加载数据 |
| 收支切换 | 只切换显示 | 需要切换饼图数据 |
| 柱状图点击 | 无功能 | 可显示具体日期详情 |

### 7. 存钱目标页 (goals.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| "新建"按钮 | `console.log` | 需要**新建目标/预算页面** |
| "存入金额" | `console.log` | 需要**存入金额弹窗** |
| "调整目标" | `console.log` | 需要**编辑目标页面** |
| "添加预算" | 无功能 | 需要**添加预算页面** |

### 8. 待确认页 (pending.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| "确认"按钮 | 从列表移除 | 需要真正保存到交易记录 |
| "编辑"按钮 | `console.log` | 需要**编辑待确认记录页面** |
| 来源开关 | 只切换状态 | 需要保存设置 |

### 9. 记账面板 (RecordSheet.vue)

| 按钮/链接 | 当前状态 | 需要实现 |
|-----------|---------|---------|
| 日期选择 | `// TODO` | 需要**日期选择器** |
| 账户选择 | `// TODO` | 需要**账户选择弹窗** |
| 标签选择 | `// TODO` | 需要**标签选择功能** |

---

## 二、缺失页面汇总

需要新建的页面：

| 页面 | 路由 | 优先级 | 说明 |
|------|------|--------|------|
| 交易详情/编辑 | `/pages/transaction/detail` | **高** | 查看和编辑单笔交易 |
| 预算管理 | `/pages/budget/budget` | 中 | 管理分类预算 |
| 存钱目标列表 | `/pages/goal/goal` | 中 | 查看所有存钱目标 |
| 分类管理 | `/pages/settings/category` | 中 | 自定义收支分类 |
| 账户管理 | `/pages/settings/account` | 中 | 管理资产账户 |
| 添加账户 | `/pages/settings/account-add` | 中 | 添加新账户 |
| 主题设置 | `/pages/settings/theme` | 低 | 切换主题颜色 |
| 提醒设置 | `/pages/settings/reminder` | 低 | 记账提醒配置 |
| 搜索 | `/pages/search/search` | 低 | 全局搜索交易 |

---

## 三、待完善功能

1. **数据持久化** - 当前使用 Pinia + persistedstate，刷新后数据保留，但清除浏览器数据会丢失
2. **图表交互** - 报表页的柱状图和饼图是静态的，需要交互
3. **动画效果** - 页面切换动画、列表项动画
4. **错误处理** - 网络错误、数据验证等
5. **国际化** - 当前只有中文

---

*文档版本：v1.0*
*整理时间：2026-05-28*
